apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-crm
  labels:
    app: demo-crm
spec:
  replicas: {{ .Values.demo-crm.replicasCount }}
  selector:
    matchLabels:
      app: demo-crm
  template:
    metadata:
      labels:
        app: demo-crm
    spec:
      containers:
      - name: demo-crm
        image: {{ .Values.demo-crm.imageName }}:{{ .Values.demo-crm.imageTag }}
        ports:
        - containerPort: {{ .Values.demo-crm.appPort }}
        envFrom:
          - configMapRef:
              name: configmap
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
