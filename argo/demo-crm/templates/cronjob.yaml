apiVersion: batch/v1
kind: CronJob
metadata:
  name: clear
  namespace: default
spec:
  schedule: "* 0 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: clear
            image: mongo
            command: ["/bin/sh","-c"]
            args:
            - mongosh --host "demo-crm-mongodb-headless" --username "root" -p "Marcin7443" --eval "db.clients.drop()"
          restartPolicy: OnFailure

